{
  // point VS Code’s Python extension at the venv
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",

  // include venv-local kernels in discovery
  "jupyter.kernelSpecPath": ["${workspaceFolder}/.venv/share/jupyter/kernels"]
}
